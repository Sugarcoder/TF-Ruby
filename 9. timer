class Timer

	def TimeInitialize

		attr_accessor :seconds        # attr_accessor created so "seconds" could be read and written

		@seconds = 0

	end

	def time_string

		hours = @seconds/3600     # 60 seconds * 60 minutes = 3600 seconds in 1 hours
		
		seconds_leftover = @seconds % 3600

	end

end

