class Timer

	def TimeInitialize

		attr_accessor :seconds        # attr_accessor created so more "seconds" can be added

		@seconds = 0

	end

	def time_string

		hours = @seconds/3600     # 60 seconds * 60 minutes = 3600 seconds in 1 hours
		
		remainder = @seconds % 3600

	end

end

